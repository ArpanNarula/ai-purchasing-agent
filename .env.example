# Optional. The application runs in deterministic demo mode without an API key.
OPENAI_API_KEY=

# Change to an available tool-capable model when using live mode.
OPENAI_MODEL=gpt-5-mini

# demo | openai
AGENT_MODE=demo
